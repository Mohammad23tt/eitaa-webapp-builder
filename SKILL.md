---
name: eitaa-webapp-builder
description: استفاده از این مهارت برای ساخت، توسعه، دیباگ و انتشار برنامک (Web App / Mini App) برای پیام‌رسان ایتا. تریگرها شامل ایتا، eitaa webapp، برنامک ایتا، مینی‌اپ ایتا، ساخت بازی ایتا، SDK ایتا، eitaa-web-app.js، ایتایار، ثبت برنامه در ایتایار، توسعه وب‌اپ ایتا، بازی وب‌اپ برای ایتا.
---

# سازنده برنامک ایتا (Eitaa WebApp Builder)

## بررسی اجمالی

این مهارت راهنمای کامل و عملی برای ساخت برنامک‌های تحت وب (Web App) قابل اجرا داخل پیام‌رسان ایتا است. شامل ثبت در ایتایار، اتصال SDK رسمی، استفاده از متدهای کلیدی، بهترین شیوه‌های امنیتی، پشتیبانی از RTL و فارسی، ساخت بازی و اپ ساده، دیباگ و استقرار می‌شود.

## جریان کاری استاندارد

۱. **ثبت برنامه در ایتایار**
   - کاربر را به https://eitaayar.ir هدایت کن.
   - مراحل: احراز هویت توسعه‌دهنده → برنامه‌ها → افزودن برنامه.
   - فیلدهای ضروری: عنوان، نام کاربری برنامه، آدرس مقصد (HTTPS بدون ریدایرکت)، عبارت دکمه، تصویر پروفایل، پیام شروع.
   - پس از ثبت، توکن برنامه و شناسه را یادداشت کن.

۲. **ساختار پروژه پایه**
   - حداقل فایل‌ها: `index.html` + `app.js` (یا فریمورک دلخواه).
   - همیشه SDK را از منبع رسمی بارگذاری کن (هرگز لوکال دانلود نکن):
     ```html
     <script src="https://developer.eitaa.com/eitaa-web-app.js"></script>
     ```
   - `lang="fa" dir="rtl"` و فونت فارسی (مثل Vazir) توصیه می‌شود.
   - viewport و متا تگ‌های موبایل را درست تنظیم کن.

۳. **اتصال و مقداردهی اولیه SDK**
   - پس از لود صفحه:
     ```js
     const webApp = window.Eitaa?.WebApp;
     if (!webApp) {
       console.warn("SDK ایتا در دسترس نیست — فقط داخل محیط ایتا کار می‌کند");
       return;
     }
     webApp.ready();          // اعلام آماده بودن
     webApp.expand();         // گسترش به حداکثر ارتفاع (اختیاری اما توصیه‌شده)
     ```
   - اطلاعات کاربر فقط از `initData` (رشته خام) در سمت سرور اعتبارسنجی شود. هرگز به `initDataUnsafe` اعتماد نکن.

۴. **متدها و قابلیت‌های پرکاربرد**
   - بستن: `webApp.close()`
   - هشدار / تأیید / پاپ‌آپ: `showAlert`, `showConfirm`, `showPopup`
   - دکمه اصلی پایین: `MainButton.setText()`, `.show()`, `.hide()`, `.onClick()`
   - دکمه برگشت: `BackButton.show()`, `.onClick()`
   - درخواست شماره تماس: `requestContact(callback)`
   - تغییر رنگ هدر و پس‌زمینه: `setHeaderColor()`, `setBackgroundColor()`
   - رویدادها: `onEvent('themeChanged', ...)`, `viewportChanged` و غیره
   - بازخورد لمسی: `HapticFeedback.impactOccurred('medium')` و مشابه

۵. **امنیت و اعتبارسنجی**
   - `initData` را حتماً در بک‌اند با توکن برنامه اعتبارسنجی کن (مشابه الگوریتم Telegram WebApp).
   - همه ارتباطات با سرور خودت فقط از طریق HTTPS.
   - داده‌های حساس را سمت کلاینت نگه ندار.
   - برای ارسال پیام از برنامه به کاربر از API ایتایار استفاده کن (`https://eitaayar.ir/api/app/sendMessage`).

۶. **استقرار و تست**
   - هاست باید HTTPS معتبر داشته باشد (GitHub Pages، Vercel، Netlify، یا سرور شخصی).
   - تست واقعی فقط روی نسخه اندروید و دسکتاپ ویندوز ایتا ممکن است.
   - برای دیباگ اندروید: Enable WebView Debug در ایتا + chrome://inspect
   - برای دسکتاپ: تنظیمات پیشرفته → Enable webview inspecting

۷. **الگوهای رایج برای بازی و اپ**
   - بازی ساده: استفاده از canvas یا DOM + ذخیره امتیاز با localStorage یا ارسال به سرور.
   - اپ تعاملی: ترکیب MainButton + showPopup + requestContact.
   - تم تاریک/روشن: گوش دادن به `themeChanged` و اعمال `themeParams`.

## بهترین شیوه‌ها

- همیشه SDK را از آدرس رسمی بارگذاری کن.
- از ابتدا `webApp.ready()` و در صورت نیاز `expand()` را صدا بزن.
- رابط کاربری را برای صفحه باریک موبایل و RTL بهینه کن.
- برای بازی‌ها از requestAnimationFrame و مدیریت صحیح viewportStableHeight استفاده کن.
- پس از ساخت، لینک تست و دستورالعمل ثبت در ایتایار را به کاربر بده.
- اگر کاربر عکس یا کد موجود دارد، ابتدا آن را تحلیل کن و سپس پیشنهاد بهبود بده.

## منابع رسمی

- مستندات SDK: https://developer.eitaa.com و https://github.com/eitaa/Documentation
- مثال سریع: https://developer.eitaa.com/docs/ExamplesAndPackages/QuickExample/
- پنل ثبت برنامه: https://eitaayar.ir

## خروجی مورد انتظار

- کد کامل و آماده اجرا (HTML + JS یا فریمورک)
- توضیح گام‌به‌گام ثبت در ایتایار
- نکات امنیتی و دیباگ
- در صورت درخواست، نسخه بازی ساده یا اپ تعاملی با کامنت‌های فارسی

هنگام تولید کد، از فونت فارسی، dir="rtl" و پشتیبانی کامل از تم ایتا استفاده کن. اگر کاربر بخواهد اسکیل را گسترش دهد یا نسخه خاصی (مثلاً فقط بازی) بسازد، مطابق آن عمل کن.
